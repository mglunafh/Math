\subsection{‘ундаментальна€ группа}
\begin{defn}
≈сли $f:I \to X$ --- путь, то гомотопическим классом этого пути будем называть множество всех св€занно гомотопных ему путей, а обозначать будем через $[f].$
\end{defn}
¬ силу леммы о произведении гомотопных путей можно определить произведение гомотопических классов на представител€х: $[f]\cdot[g] = [f\cdot g].$ ѕосто€нный класс $[x_0]$ --- это класс константного пути в точку $x_0$.

\begin{defn} ћножество всех гомотопических классов петель пространства $X$ с началом и концом в $x_0$ обозначим через $\pi_1(X,x_0).$ 
\end{defn}

\begin{thm} ћножество $\pi_1(X,x_0)$ образует группу. Ќаучное название --- фундаментальна€ группа пространства $X$ с отмеченной точкой $x_0$. 
\end{thm}